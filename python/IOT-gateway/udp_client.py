import sysimport socketimport machinefrom machine import ADCimport timeimport networkadc = ADC(0)def adcSocket():  # create a socket object  s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)  #host = "tcp.tlink.io" # IP-address of the TCP server of NodeMCU  host = "112.74.142.132"  port = 8647  #s.connect((host, port))  s = socket.socket()  s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)  s.connect((host,port))  '''  s=socket.socket(socket.AF_INET, socket.SOCK_DGRAM)  s.setsockopt(socket.SOL_SOCKET,socket.SO_REUSEADDR,1)    s.bind((host,port))  '''  s.send("2O78E783Q8I9NU1H") #第一次连接后，需要发送序列号，仅一次即可  time.sleep(2)  #延时2S  i=10 #发送十次  while(i>0):    print(i)    i-=1    lux = adc.read()    print(lux)        lux="#"+str(lux)+"#" #组合出上送的数据标签    s.send(lux)    print(lux)    time.sleep(2) #延时2S 
    '''
    while True:      data = s.recv(1024)      if(len(data) == 0):        print("close socket")        s.close()        break      print(data)      ret = s.send(data)
    '''  